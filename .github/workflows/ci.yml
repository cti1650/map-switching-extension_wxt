name: CI

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-typecheck:
    name: Lint & TypeScript
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Action
        uses: ./.github/workflows/actions/setup

      - name: Check formatting
        run: yarn format

      - name: Run linter
        run: yarn lint

      - name: TypeScript check
        run: yarn compile

  unit-test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Action
        uses: ./.github/workflows/actions/setup

      - name: Run unit tests
        run: yarn test

  e2e-test:
    name: E2E Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Action
        uses: ./.github/workflows/actions/setup

      - name: Setup Browser Action
        uses: ./.github/workflows/actions/setup-browser

      - name: Run extension build for E2E tests
        run: yarn build

      - name: Run E2E tests
        run: yarn e2e
        timeout-minutes: 1

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  build:
    name: Build Extension
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck, unit-test]
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Action
        uses: ./.github/workflows/actions/setup

      - name: Build Chrome extension
        run: yarn build

      - name: Build Firefox extension
        run: yarn build:firefox

      - name: Create extension packages
        run: |
          yarn zip
          yarn zip:firefox

      - name: Upload Chrome build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: chrome-extension
          path: .output/*-chrome.zip
          retention-days: 30

      - name: Upload Firefox build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firefox-extension
          path: .output/*-firefox.zip
          retention-days: 30

  security-check:
    name: Security Scan
    runs-on: ubuntu-latest
    if: false # TODO: 参考用に残す。後で有効化すること。
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Action
        uses: ./.github/workflows/actions/setup

      - name: Run security audit
        run: yarn audit --level critical

      - name: Check for known vulnerabilities
        run: |
          npx audit-ci --config ./audit-ci.json || true
